一、基础
	$变量名 = xxx	赋值
	echo ，print输出
	大部分与js语法相同，
		数组：	数值数组：
				格式：$a = array("a", "b", "c");
				访问：echo $a[0]	通过下标访问；也可以修改	
			关联数组：
				格式：$a = array("name"=>"李明", "age"=>16);
				访问：echo $a["name"];	通过键访问对应的值
	$_GET["get"]	获取前端发送过来的数据
